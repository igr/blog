{% extends "base.njk" %}

{% block main %}

  <article class="post">
    <header class="post-header">
      <h1 class="post-title">{{ title }}</h1>
      <time class="post-date skew" datetime="{{ date | dateISO }}"><span>{{ date | rsDateLong }}</span></time>
      <div id="readtime" class="skew"><span>{{ readingTime.minutes | round(0, "ceil") }} min</span></div>
    </header>

    {% include "partials/share-links.njk" %}

    {{ content | safe }}

  </article>

  <ul class="tags">
    {% for t in tags %}
      {% set tagPage = site.pageOfCollection('tags', t) %}

      <li class="tag"><a href="{{ tagPage.url }}">{{ t }}</a></li>
    {% endfor %}
  </ul>

{% endblock %}



{% block footerfiles %}
<script>
hljs.configure({tabReplace: '    '});
hljs.initHighlightingOnLoad();
</script>
{% endblock %}
